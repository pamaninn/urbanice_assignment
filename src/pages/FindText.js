import { Box } from "@mui/material";
import React, { useState, useEffect } from "react";

function Find์NumberPattern() {

    const [result, setResult] = useState([]);
    const inputVal = [X, 5, 9, 15, 23, Y, Z];
    const findXYZ = () => {  
        
    let Tn = Tn-1 + 2(n-1);
        
      };

      useEffect(() => {
        findXYZ();
      }, []);


    return (
        <Box sx={{ flexGrow: 1, mt: 2 }}>
{result}
        </Box>
        );
    }
    
    export default Find์NumberPattern;